# @bsky-kampagnen-bot/shared-ui

Erste Sammlung gemeinsamer React-Komponenten. Aktuell enthält das Paket `NewPostsBanner`, einen anpassbaren `Button`, generische `Card`-Wrapper sowie `Modal` und `MediaDialog`; weitere Module folgen während des Refactorings.
