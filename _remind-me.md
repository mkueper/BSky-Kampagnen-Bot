1. Link-Vorschau direkt im Client-Composer ausgeben und einen integrierten Proxy/Fallback bereitstellen, damit niemand lokal einen eigenen Preview-Service konfigurieren muss.
2. Suche
   1. Wenn ein Suchbegriff in der Eingabeleiste steht, soll das "x" zum Leeren der Leiste immer sichtbar sein und nicht nur, wenn die Maus über dem Input ist.
   2. Die Button samt Combobox im Eingabefeld der Suche soll entfallen.
   3. Die aufklappenden Prefixauswahl bei der Eingabe von ":" soll mit den Tasten durchscrollbar sein und der markierte Eintrag soll bei Enter in die Suchleiste übernommen werden. das weotere Vorgehen bleibt wie gehabt.
3. In den Composern sollten eingefügte Handle, sofern gültig, als Link angezeigt werden. Sie müssen nicht klickbar sein.
4. Wir haben im Home und in Mitteilungen keine Bereichs-Header (wie ich sie nenne), und fangen im home direkt mit den Tabs an. Das sollten wir unter Mitteilungen ebenso handeln.
5. Alle Header sollten dieselbe Höghe bekommen (springen der Header beim Umschalten beseitigen)
6. ✅ Antworten lassen sich nun direkt in den Thread-Composer überführen (Button „In Thread umwandeln“) – bestehender Text/Medien wandern automatisch in den ersten Abschnitt.
7. 
