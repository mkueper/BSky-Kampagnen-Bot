import "dotenv/config";
export declare const env: {
    bluesky: {
        serverUrl: string;
        identifier: string;
        appPassword: string;
    };
    mastodon: {
        serverUrl: string;
        token: string;
    };
};
