{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --config ../vitest.config.mjs"
  }
}