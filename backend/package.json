{
  "name": "backend",
  "version": "1.1.1",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --root .."
  },
  "dependencies": {
    "express-async-errors": "^3.1.1",
    "helmet": "^8.1.0"
  }
}
