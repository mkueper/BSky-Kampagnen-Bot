# SQLite benötigt glibc; daher nutzen wir eine Debian-basierte Node-Variante.
FROM node:22-slim
WORKDIR /app

# Dependencies exakt wie im Lockfile installieren (inkl. Build-Schritte der nativen Module).
COPY package*.json ./
RUN npm ci --omit=dev

COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
