{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-dashboard",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/dashboard",
        "env": {
          "npm_config_workspaces": "true"
        }
      },
      "problemMatcher": []
    }
  ]
}
