| Datei | Status | Begründung |
|-------|--------|-----------|
| `docs/diagramme/systemarchitektur.md` | konsistent | Das Diagramm spiegelt die aktuelle Architektur mit Express-API, Scheduler, SSE, SQLite sowie Bluesky/Mastodon/Tenor wider, wie sie in `backend/server.js`, `backend/src/config.js` und den Frontend-Projekten implementiert ist. |
| `docs/development/listView-architecture.md` | konsistent | Verweist auf `bsky-client/src/context/reducers/listView.js`, `modules/listView/listService.js`, `hooks/useListPolling.js` und die Timeline-/Notifications-Module, die im Code existieren und die beschriebenen Rollen erfüllen. |
| `docs/development/listView-checklist.md` | konsistent | Die Checkliste baut auf der in `listView-architecture.md` beschriebenen Struktur auf und referenziert Dateien (z. B. `ClientApp.jsx`, `listView`-Reducer, `listService`, `useListPolling`), die im aktuellen BSky-Client vorhanden sind. |
| `docs/development/vscode-workspace.md` | konsistent | Beschreibt die Workspace-Datei `BSky-Kampagnen-Bot.code-workspace`, Launch-Konfigurationen und Tasks (`npm run dev`, `npm run install:frontend`), die im Projekt vorhanden und so nutzbar sind. |
| `docs/layout-guide.md` | konsistent | Der Leitfaden bezieht sich auf die Pane-Struktur (NAV/Content/Detail), `bsky-scroll-container`, `ScrollTopButton` und shared-ui-Bausteine, die im BSky-Client und Dashboard tatsächlich so verwendet werden. |
| `docs/ui.md` | konsistent | Listet konkrete UI-Bausteine (`Card`, `Button`, `Badge`, `SummaryCard`, `ActivityPanel`, `FloatingToolbar`, `ToastProvider/useToast`) mit Pfaden, die existieren, und beschreibt Patterns (Pane-Aufteilung, SSE-Hooks) korrekt. |
| `docs/konventionen/ui-konventionen.md` | konsistent | Beschreibt UI-Prinzipien, Komponentenklassen und Pane-Zuordnung in Übereinstimmung mit den tatsächlich vorhandenen Layout-/Component-Strukturen im Dashboard und BSky-Client. |
| `docs/konventionen/ui-anweisungen.md` | konsistent | Die Prompt-Anweisungen referenzieren vorhandene UI-Bausteine und Regeln (Tailwind, shared-ui, Hooks, Pane-Layout), ohne im Widerspruch zur aktuellen Implementierung zu stehen. |
| `docs/konventionen/test-anweisungen.md` | teilweise inkonsistent | Die Vitest-/Testing-Library-Vorgaben passen, aber der implizierte Aufbau (Tests streng neben den Dateien) weicht von der tatsächlichen Backend-Teststruktur unter `backend/tests/**` ab. |
| `docs/konventionen/test-konventionen.md` | teilweise inkonsistent | Das Dokument fordert Tests ausschließlich in `__tests__`-Ordnern neben den Quellfiles, während reale Tests (insbesondere im Backend) in separaten Pfadstrukturen (`backend/tests/**`) liegen; andere Konventionen (Vitest, *.test.*) stimmen. |

